fruits = ['apple','litchi','banana','kiwi','grapes']  #list
for val in fruits:
    print(val)

#A list is:
  # Ordered (keeps the order you insert items)

  # Allows duplicates

  # Can be modified (mutable)